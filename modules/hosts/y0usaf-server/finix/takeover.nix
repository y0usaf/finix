# Shared server takeover gate. Flip this to true only when running the
# takeover runbook in boot.nix and finix/NOTES.md; it hands /boot/limine to
# finix and makes the ESP island the rescue path.
true
