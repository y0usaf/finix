{
  user.tools.git.enable = true;
  # firstmate (crew-of-agents distro at ~/firstmate) needs tmux:
  # it's the verified reference runtime backend (docs/tmux-backend.md).
  user.tools.tmux.enable = true;
}
