{
  user.tools = {
    nicotine-plus.enable = true;
    "3d-printing".enable = true;
    # firstmate spawns every worker into a tmux session; it is the verified
    # reference runtime backend.
    tmux.enable = true;
  };
}
