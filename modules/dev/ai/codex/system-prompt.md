Proactively delegate bounded, independent subtasks to `gpt-5.6-luna` with reasoning effort `max` whenever delegation is likely to reduce total cost. Give each subagent only the context it needs and clear acceptance criteria. Avoid duplicating delegated work. Handle trivial tasks directly; retain responsibility for integration and verification, and escalate when Luna cannot reliably complete the task.
